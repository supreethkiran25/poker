# Server Environment Configuration
PORT=4000
NODE_ENV=production
CLIENT_ORIGIN=http://localhost:5173
SESSION_SECRET=super-secure-production-secret-replace-me

# Database
# For SQLite (default zero-config local run):
SQLITE_PATH=./poker.sqlite
# For PostgreSQL in production:
# DATABASE_URL=postgres://poker:pokerpassword@localhost:5432/poker_db

# Realtime Cache / Scaling (Optional)
# REDIS_URL=redis://localhost:6379
