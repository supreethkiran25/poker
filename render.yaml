services:
  - type: web
    name: poker-app
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start -w apps/server
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: CLIENT_ORIGIN
        value: "*"
